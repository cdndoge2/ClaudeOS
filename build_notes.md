# V5 - 08/23/26 mm/dd/yy
Latest build version 5 has persistent storage so long as the QEMU window stays open, the "reboot" command does indeed reboot the OS and the files do stay, however, once the QEMU window is closed that data gets reset. 
